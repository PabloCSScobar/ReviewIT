import { PaginationOptions } from "api-interfaces";

export const PAGINATION_OPTIONS: PaginationOptions = {
    page: 1,
    itemsPerPage: 2,
};

export const USER_REPUTATION_OPTIONS = {
    addAnswer: 5,
    addPost: 10,
    bestAnswer: 15,
}